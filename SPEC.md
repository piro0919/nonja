# 仕様

壁打ちで決まったことを置く場所。決まっていないものは「保留」に残す。

## なぜ作るのか

macOS の通知が二つの意味で使いものにならない。

一つは、バナーが毎回割り込んでくること。作業中に画面の右上を奪われる。

もう一つは、通知センターが見づらいこと。全アプリが時系列で混ざって並ぶだけで、
既読の概念が無く、まとめて処理する手段も無い。数が増えると重要なものが埋もれる。

結果として、見逃したくないものを見逃し、どうでもいいものを消す作業だけが残る。

道具を替える。バナーは出さず、届いたものは裏で溜め、自分のタイミングでまとめて見る。
そして放っておいたものは勝手に片付く。

## 決まったこと

### 名前

**Nonja（ノンジャ）。** Notification + Ninja。

忍者は気配を消す存在で、バナーを出さずに裏で通知を捌くという中身と直結する。

- 同名のアプリは見つからなかった。近いのは NYNJA（通信アプリ）と Noja（業務管理アプリ）で、
  どちらも別分野なので衝突しない
- **Ninja と綴りが近いことは承知のうえで採る。** 検索が混ざる可能性と、
  英語圏で「ノン・ジャ」と読まれる可能性があるが、どちらも実害は小さい
- 見送った候補
  - Tayori — PR TIMES のカスタマーサポートサービスが十年動いている
  - Sotto — macOS の音声入力アプリが実在する
  - Tamari / Shirase / Nagame — 空いてはいたが、由来を説明できる Nonja を採った

Gocci、Okigae と同じく名前が機能を説明しないので、**アイコンが意味を引き受ける**。

### 通知の取得方法

**通知センターのデータベースを直接読む。**

```
~/Library/Group Containers/group.com.apple.usernoted/db2/db
```

macOS 26.5.2 で実機検証済み。`record` テーブルに一件ずつ入っており、
`data` 列のバイナリ plist を開くと必要なものが全部入っている。

```
"app"  => "com.apple.ScriptEditor2"
"date" => 808343401.463639
"req"  => { "titl" => "AX検証", "subt" => "到着監視の確認", "body" => "本文テスト" }
"uuid" => {length = 16, bytes = 0xdd96b2dabe1d4cdbbd0ba4cc73d59868}
```

- **`titl` / `subt` / `body` は文字列とは限らない。** ローカライズされた通知は
  `[書式, 解決済みの文字列, 引数]` の配列で入っている。解決済みを優先して採る。
  KeepingYouAwake が実例で、文字列だけを見ていると本文が空になる
- 送信元アプリが起動しているかは無関係。終了したアプリの通知も読める
- `delivered_date` は Apple 基準時なので `+978307200` で Unix 時間になる
- 読むのは必ず読み取り専用（`?mode=ro`）で開く。書き込みはしない

#### 検討して捨てた経路

| 経路 | 結果 |
| --- | --- |
| `/var/folders/…/0/com.apple.notificationcenter/db2/` | **空**。`db-shm` と `db-wal` の残骸のみで本体が無い。ネット上の記事はここを指しているが現行 OS では機能しない |
| アクセシビリティで通知センターを読む | 読めるが、パネルを画面に開いている間だけ。閉じていると窓がゼロ。裏で溜め続けられない |
| バナーをアクセシビリティで捕まえる | バナー表示中は読めるが、バナーを出す設定が前提になり、そもそもの動機と矛盾する |

### 元アプリへの遷移

**クリックされた時だけアクセシビリティで本物の通知を押す。**

データベースの `uuid` と、通知センター上の要素の `AXIdentifier` が一致する。

- データベース側 `dd96b2da-be1d-4cdb-bd0b-a4cc73d59868`
- 画面側 `DD96B2DA-BE1D-4CDB-BD0B-A4CC73D59868`

普段は裏でデータベースを読んで一覧を作り、ユーザーがクリックした瞬間だけ
通知センターを開いて該当要素に `AXPress` を送る。`AXPress` は本物のクリックそのものなので、
遷移は OS が本来やる動きになる。

これにより「バナーを出さない」と「元アプリに飛べる」が両立する。

制約として、**飛べるのは通知センターにまだ残っている通知だけ**。
すでに消去された通知は本文だけ読めて遷移はできない。この場合はアプリを起動するだけに留める。

### 到着の検知

**データベースの更新監視。** 反映まで約5秒の遅れがある（実測）。

受信箱として使う分には影響しない。1時間溜めるルールに5秒のずれは効かない。

### 必要な権限

- **フルディスクアクセス** — データベースの読み取りに必須
- **アクセシビリティ** — クリック時の遷移に必須

どちらも外れることがある。macOS Monterey 12.6.1 以降、再起動や OS 更新で
権限が消える報告が続いている。またアプリの署名が変わると必ず外れる。

**起動時に両方の可否を確認し、駄目なら設定画面への導線を出す。**
権限が無い状態でも、あるものだけで動く形にしておく。

| 権限の状態 | 動作 |
| --- | --- |
| 両方あり | 通常動作 |
| フルディスクアクセスのみ | 一覧は出る。クリックしても飛べない |
| アクセシビリティのみ | 通知センターを一瞬開いて読み込む縮退動作 |
| 両方なし | 案内画面のみ |

署名は開発者 ID で固定し、公証を通す。アプリ更新のたびに権限が外れる事態を避ける。

### 画面

**メニューバー常駐。印をクリックすると、その真下に一覧の窓が出る。**

### 件数は出さない

**アプリのどこにも数字を出さない。** メニューバーも、束の見出しも、窓の下の表示も。
実装漏れではなく、意図した選択。

138 件でも 5 件でも、やることは開いて見るだけで、数字は判断を変えない。
このアプリの動機が鬱陶しさを減らすことなので、バッジの数字が視界の端で増えていくのは
そもそも逆を向いている。幅が伸び縮みするのも落ち着かない。

代わりに**有無だけ**を示す。溜まっているときはベルを塗りつぶし、無いときは輪郭だけにする。
窓の下も「溜まっています」「溜まっていません」の二つだけ。

### 既読は一覧から消えること

**未読と既読を見た目で区別しない。一覧に出ているものが未処理、消えたものが処理済み。**

はじめは既読の印（点と文字の濃さ）と「片付ける」を別々に持っていたが、外した。
既読にしても一覧に残るなら埋もれる問題は解決せず、結局あとで片付けることになる。
また「片付ける」という言葉は何が起きるか伝わらない。

いまは「既読」ひとつで、押すと一覧から消える。元アプリへ飛んだものも同じく下ろす。
開いた時点で用は済んでいるため。

**一覧はアプリごとにまとめる。** 時系列に混ぜると通知センターと同じで埋もれる。
束の並びは、その束の一番新しい通知の時刻で決める。
見出しには折りたたみと「既読」だけを置く。

行には送信元アプリのアイコンを出す。どこから来たのかが一目で分かる。
背景はぼかしの素材、選択は角丸の薄い面。**濃く塗ると中の文字が沈むので薄く留める。**

**開いた直後はどの行も選ばない。** 先頭を自動で選ぶと、一行だけ青く塗られた理由が伝わらない。
矢印キーを押した時点で表が自分で先頭を選ぶので、操作は変わらない。

操作は、矢印で移動、Return で元アプリへ、Delete で片付け、上の欄で絞り込み。

メニューバーの印は絵ではなく**図形として描く**（`Mark.swift`）。
生成した絵を 18 ピクセルまで縮めると輪郭がギザつき、左右の非対称も目立った。
スリットは丸めた端を胴の中に置くと両脇に出っ張りが残るので、胴より外まで伸ばした
真っ直ぐな帯で断ち切る。

### 振り分けルール

**アプリ単位。** 上から順に評価し、最初に一致したものを適用する。

各ルールで指定するもの。

- 対象アプリ
- 扱い — すぐ見せる / 溜める / 自動で既読にする
- 溜める場合の保持時間

正規表現による絞り込みは、アプリ単位で足りなくなってから足す。最初の版には入れない。

### 片付けた通知の控えは持たない

**アーカイブは作らない。** 一度は入れたが、外した。

通知は本来その場限りの情報で、後から見返す機会がほとんどない。しかも控えの通知は
通知センターからも消えているためクリックしても飛べず、本文が読めるだけになる。
価値が半分になった控えのために、画面にタブを一つ常設する釣り合いが取れていなかった。

元の通知は macOS の通知センターにそのまま残るので、本当に必要なら通知センターを開けば辿れる。
**二重に抱えない。** 必要になったら足す。

### 作り方

**素の Swift。Gocci と同じ構成。**

Xcode プロジェクトを持たず、`Sources/` に Swift を並べて `build.sh` で `.app` を組み立てる。
Sparkle で自動更新。署名と公証は `release.sh`。

やることの中身が OS に密着しているため、Tauri だと結局 Swift 側を書いて
橋渡しすることになり二重管理になる。

### 署名

**「Okigae Dev」で署名する。** 暫定署名（`codesign --sign -`）は同一性がビルドごとに変わり、
フルディスクアクセスとアクセシビリティの許可が作り直すたびに外れて開発が進まない。

固定した証明書で署名すると指定要件が
`identifier "io.kkweb.nonja" and certificate leaf = H"97e060…"` になり、
再ビルドしても許可が保たれることを実機で確認した。

### ログイン時の自動起動

**`SMAppService.mainApp`。** 補助の実行ファイルを持たずにアプリ自身を登録できる。
設定画面のチェックで切り替え、切り分け用に `--login on` / `--login off` も用意した。

常駐して初めて意味が出るアプリなので、毎回手で起動する状態だと使われない。

登録はアプリの場所を覚える。**`/Applications` へ移すときは、いったん解除してから移動し、
登録し直す。** そうしないと古い場所を指したままになる。

### 公証は通さない

**Apple の開発者 ID は取らない。** 年 99 ドルは、自分用のアプリ1本には見合わない。

引き換えに、利用者は初回だけシステム設定で「このまま開く」を押すことになる。
macOS Sequoia で右クリックの「開く」による回避は無くなったので、経路はそこだけ。
一度許可すれば以降は出ない。

**使えないわけではなく、最初の一手間と「開発元を検証できません」という警告を
利用者に負わせる**という取引。事情の分かる相手に配る分には足りる。
README に手順を書いてある。

### 自動更新

**Sparkle。確認は起動時に1回だけで、定期的には見に行かない。**
見つかったときだけ画面を出す。通知の鬱陶しさを減らすためのアプリが、
自分の更新で割り込んでは筋が通らない。

鍵は Gocci・Konechi と同じものをログインキーチェーンから使う。
Sparkle の鍵はアプリ単位ではなくキーチェーン単位で作られる。**失うと更新を配れなくなる。**

配布は `./release.sh <版数>`。ビルド、自己テスト、DMG と zip、appcast の署名、
GitHub Releases への登録までを通す。

### 画面を組むときの落とし穴

**位置を制約で決める部品は `translatesAutoresizingMaskIntoConstraints` を必ず切る。**
切り忘れると自動生成の制約とぶつかり、**その部品ではなく周り全体**が崩れる。
設定画面で二度やった。症状は「部品の高さが 2 や 0 に潰れ、窓の外へはみ出す」。

崩れているかどうかは、画面写真より**部品の位置と大きさを読む**ほうが速い。

```
osascript -e 'tell application "System Events" to tell process "Nonja" \
  to tell window "Nonja の設定" to get {description, position, size} of every UI element'
```

窓は重なりや別ディスプレイへの移動で写真がずれる。メニューバーの印は操作している画面へ移り、
窓もそちらに付いていく。設定の窓は一覧の窓と同じ画面に出す（`center()` だと別の画面へ飛ぶ）。

### 自己テスト

**`./Nonja.app/Contents/MacOS/Nonja --selftest`。** 画面を触らずにルールの当たり方を確かめる。
直したら毎回これを通す。ほかに `--dump`（読み取りの確認）と `--press <uuid>`（遷移の確認）がある。

## 保留

### 「確認した」の定義

「一定時間確認しなかったら消す」の判定基準が決まっていない。

- **表示された時点**とみなす — アプリを一度開けば全部が確認済みになり、自動削除がほぼ働かない
- **クリックした時点**とみなす — 触らなかったものは時間切れで消えていく

両方を実装済みで、設定画面から切り替えられる。**既定はクリック基準。**
実際に使ってから決める。

### 保持時間の既定値

1時間を仮置きとする。実際に使ってみて決める。

### ルールの上限本数

未定。

## 見送ったもの

- **アーカイブ** — 上記のとおり外した
- **正規表現での絞り込み** — アプリ単位で足りなくなってから
- **バナーを捕まえて即座に閉じる方式** — 到着時刻は正確になるが、元の通知を消すので飛べなくなる

## 前提として抱えるリスク

- **非公開の仕組みに依存している。** データベースの場所も構造も Apple が公開したものではなく、
  OS 更新で壊れる可能性が常にある。実際、旧来の `/var/folders` 配下の経路は既に死んでいる
- **権限が外れる。** 上記の通り、外れる前提で縮退動作を用意する
- 壊れた時に無言で止まらないよう、**読めなくなったことを検知して画面に出す**
